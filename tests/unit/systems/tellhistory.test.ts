import { describe, it } from 'vitest';

describe('Tell History System — PARITY stubs', () => {
  it.todo('should store tells in per-letter array (a-z)');
  it.todo('should retrieve tell history by letter index');
  it.todo('should cycle through letter indices');
  it.todo('should format stored tells as "X told you Y"');
});
