import { describe, it } from 'vitest';

describe('AreFileParser — PARITY stubs', () => {
  it.todo('should parse #AREA section header with name, author, vnums');
  it.todo('should parse #ROOMS section with descriptions, exits, flags');
  it.todo('should parse #MOBILES section with stats, dice, affects');
  it.todo('should parse #OBJECTS section with types, values, wearflags');
  it.todo('should parse #RESETS section with M/O/P/G/E/D/R commands');
  it.todo('should parse #SHOPS section with keeper vnum and item types');
  it.todo('should parse #SPECIALS section with spec_fun assignments');
  it.todo('should parse #PROGRAMS section with trigger types and scripts');
  it.todo('should write parsed data to JSON files in output directory');
  it.todo('should handle malformed area files gracefully');
});
