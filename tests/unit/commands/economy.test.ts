import { describe, it } from 'vitest';

describe('Economy Commands — PARITY stubs', () => {
  // All economy commands are DONE, but registration-level tests
  it.todo('registerEconomyCommands — should register all 8 economy commands');
  it.todo('list command — should work at POS_RESTING');
  it.todo('buy command — should work at POS_RESTING');
  it.todo('auction command — should work at POS_STANDING');
});
